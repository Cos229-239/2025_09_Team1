<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Card Image -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:layout_marginEnd="8dp"
        app:cardCornerRadius="12dp"
        app:cardBackgroundColor="@color/colorSecondary"
        app:cardElevation="2dp"
        app:strokeWidth="0dp">

        <!-- FIX: Add ImageView inside the card -->
        <ImageView
            android:id="@+id/collectionCardImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Collection card image" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Brand/Scientific Name -->
    <TextView
        android:id="@+id/collectionCardBrand"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:text="Brand"
        android:textColor="@android:color/white"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="2dp"
        android:textSize="10sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Card Name -->
    <TextView
        android:id="@+id/collectionCardName"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Card name"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_marginLeft="2dp"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>